# The Hidden Hand

Prototype local em HTML, CSS e JavaScript modular.

Rode a partir da raiz do projeto com um servidor local simples:

```powershell
node scripts/static-server.cjs
```

Depois abra `http://localhost:4173`.

O estado da partida e salvo em `localStorage`.
